<!DOCTYPE html>
<html>
<head>
    <title>Recon Report - {{ timestamp }}</title>
    <style>
        body { font-family: 'Courier New', monospace; background: #1a1a1a; color: #00ff00; }
        .container { max-width: 1200px; margin: 0 auto; padding: 20px; }
        .section { margin-bottom: 40px; padding: 20px; border: 1px solid #555; }
        .vuln { color: #ff4444; background: #2a2a2a; padding: 1rem; }
        .progress { margin: 20px 0; padding: 0.5rem; background: #333; border-radius: 4px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Recon Report - {{ timestamp }}</h1>
        
        <div class="section">
            <h2>Subdomains ({{ subdomains|length }})</h2>
            <ul>
                {% for sub in subdomains %}
                <li>{{ sub }}</li>
                {% endfor %}
            </ul>
        </div>

        <div class="section">
            <h2>Vulnerabilities ({{ vulns|length }})</h2>
            {% for vuln in vulns %}
            <div class="vuln">
                <h3>{{ vuln.title }}</h3>
                <p>{{ vuln.description }}</p>
                <strong>URL:</strong> {{ vuln.url }}<br>
                <strong>Severity:</strong> {{ vuln.severity }}
            </div>
            {% endfor %}
        </div>

        <div class="section">
            <h2>Errors</h2>
            <ul>
                {% for error in errors %}
                <li>{{ error }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>
</body>
</html>